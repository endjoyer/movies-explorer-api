const invalidMovie = 'Invalid movie request data';
const noMovie = 'There is no movie with id';
const castObjectId = 'Cast to ObjectId failed';
const deleteSomeoneMovie = "You can't delete someone else's movie.";
const noUser = 'There is no user with id';
const alreadyRegister = 'A user with such a email is already registered';
const invalidUserRequest = 'Invalid user request data';
const incorrect = 'Incorrect email or password';
const notFound = 'This page not found';

module.exports = {
  invalidMovie,
  noMovie,
  castObjectId,
  deleteSomeoneMovie,
  noUser,
  alreadyRegister,
  invalidUserRequest,
  incorrect,
  notFound,
};
